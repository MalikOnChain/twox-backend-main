export enum CRYPTO_TRANSACTION_TYPES {
  DEPOSIT = 'DEPOSIT',
  WITHDRAW = 'WITHDRAW',
}

export enum WITHDRAWAL_TYPES {
  REAL = 'real',
  BONUS = 'bonus',
  CASHBACK = 'cashback',
  REFERRAL = 'referral',
  WAGER_RACE = 'wager_race',
  ALL = 'all',
}
